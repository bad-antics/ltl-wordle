// examples/demo.ltl — runnable demo for ltl_wordle

import std::io
use ltl_wordle as lib

fn main() {
    io::println("ltl_wordle demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}