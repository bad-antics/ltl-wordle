// ltl-wordle/main.ltl
//
// Wordle clone with entropy-based solver hints.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-wordle v0.1.0")
    io::println("Wordle clone with entropy-based solver hints.")
    0
}
